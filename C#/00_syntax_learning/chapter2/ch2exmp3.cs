using System;

class CircleArea {
    static void Main() {
        double radius, pi, area;
        radius = 10;
        pi = 3.1416;
        area = radius*radius*pi;
        Console.WriteLine("The area of a circle with radius 10 cm equals " + area + " cm");
    }
}
